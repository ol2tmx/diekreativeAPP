import { Component, OnInit } from '@angular/core';
// import { Calendar } from '@ionic-enterprise/calendar/ngx';

@Component({
  selector: 'app-calender-prayer-room-ionic',
  templateUrl: './calender-prayer-room-ionic.component.html',
  styleUrls: ['./calender-prayer-room-ionic.component.scss'],
})
export class CalenderPrayerRoomIonicComponent implements OnInit {

   constructor() { }
  //constructor(private calendar: Calendar) { }

//   this.calendar.createCalendar('MyCalendar').then(
//     (msg) => { console.log(msg); },
//     (err) => { console.log(err); }
//   );

    ngOnInit() {}

 }



// // import { Calendar } from '@ionic-enterprise/calendar/ngx';

// // constructor(private calendar: Calendar) { }

// // this.calendar.createCalendar('MyCalendar').then(
// //  (msg) => { console.log(msg); },
// //  (err) => { console.log(err); }
//);